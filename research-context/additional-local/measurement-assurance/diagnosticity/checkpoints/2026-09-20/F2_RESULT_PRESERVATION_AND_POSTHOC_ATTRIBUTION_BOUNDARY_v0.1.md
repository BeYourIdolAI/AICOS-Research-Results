# F2 result preservation and post-hoc attribution boundary v0.1

STATUS: `FROZEN`

FREEZE DATE: `2026-09-20`

## Original preregistered result

```text
ORIGINAL PREREGISTERED RESULT:
F2 FALSE POSITIVE / METHOD FAIL

STATUS:
PRESERVED
```

## Post-hoc attribution boundary

```text
POST-HOC ATTRIBUTION:
SEPARATE
```

Later findings may narrow or qualify mechanism attribution. They may not erase
or retroactively reclassify the preregistered result.

```text
ORIGINAL RESULT VALIDITY
≠
POST-HOC MECHANISM ATTRIBUTION
```

## Later separate findings

- D1, D2, D4, D5 and D6 applicability was `NOT ESTABLISHED` for
  `HIST-DIAG-CTRL-01`.
- D9 applicability was `ESTABLISHED`.
- One unresolved load-bearing D9-A relation can prevent PASS because D9
  requires `every` load-bearing relation to be identified before PASS/FAIL.
- D9's native outcome for the historical case is
  `UNDETERMINED FROM AVAILABLE RECORD` because the frozen rule does not resolve
  the overlap between `AMBIGUOUS` and `INVALID TRACE`.
- The translation basis from a native D9 outcome to Step 2 `UNRESOLVED` is
  `NOT ESTABLISHED`.
- Step 2 explicitly defined that its reported `UNRESOLVED` result was not
  converted to PASS and operated as blocking/non-admission.
- `PROSPECTIVE-DIAG-PROBE-01` produced:

  ```text
  X = DIAGNOSTIC
  Y = NON-DIAGNOSTIC
  X ≠ Y
  ```

- The prospective evidence maps to D9-A only.
- D9-B received no new evidence.

## Non-claims

These later findings do not establish that:

- the preregistered F2 event did not occur;
- D1–D11 are validated;
- D9 is validated or generally necessary;
- sensitivity or specificity is established;
- the applicability or translation findings supply a method patch;
- a new normative primitive is required.

No historical artifact is rewritten by this boundary record.

